# AGENT.md

이 저장소에서 코드를 고칠 때 알아야 할 것들. 사용법은 [README.md](README.md) 에 있다.

여기 적힌 숫자는 전부 **이 게임 화면에서 직접 재서 얻은 값**이다. 짐작으로 바꾸지 말고,
바꿔야 한다면 `tools/dev.py` 로 다시 재고 근거를 주석에 남겨라.

---

## 0. 처음 붙잡았다면 이 순서로

```bash
pip install -e ".[dev]"
pytest                                  # 36개. 픽스처가 없으면 전부 skip 된다.
python3 tools/dev.py diag               # 게임이 떠 있으면 연결·인식 상태가 나온다
python3 tools/dev.py goto-battle        # 팝업·절전 모드를 치우고 전투화면까지
python3 tools/dev.py identify           # 지금 화면을 퀘스트별로 몇 점으로 보는지
```

게임이 안 떠 있어도 `pytest` 와 `--from` 옵션으로 저장된 화면을 써서 대부분 확인할 수 있다.

---

## 1. 절대 어기면 안 되는 것

### 좌표는 항상 0.0~1.0

모듈·퀘스트 코드에 픽셀 좌표를 쓰지 마라. `NormRect` 와 정규화 좌표만 쓴다.
디바이스 픽셀로 바뀌는 곳은 `Context.to_device` 한 군데뿐이다.

이유: 블루스택 창 크기가 언제든 바뀐다. 템플릿도 저장 당시의 화면 대비 크기를 같이
기록해 두었다가 매칭할 때 현재 크기로 다시 스케일한다.

### 고정 대기를 새로 만들지 마라

`ctx.sleep(2.0)` 같은 코드를 추가하지 마라. 이미 전부 걷어냈다.

```python
ctx.wait_until(조건, 상한)          # 조건이 서면 즉시 반환
ctx.wait_for_template(이름, 상한)    # 버튼이 나타나면 즉시 그걸 반환
```

"0.1초 뒤 첫 확인 → 아직이면 0.3초 간격 재확인" 이 기본 동작이다. 상수는 소모 시간이 아니라
**상한**이라는 뜻으로 이름을 `*_TIMEOUT` 으로 짓는다. 고정 대기를 조건 대기로 바꾼 것만으로
퀘스트 한 바퀴가 20초에서 7~8초가 됐다.

### 재화를 쓰는 동작에는 안전장치를 남겨라

뽑기권이 모자라면 '10회' 버튼이 주황에서 청록(다이아 결제)으로 바뀐다. 템플릿이 주황 버튼이라
그때는 매칭이 실패하고 퀘스트가 멈춘다. **이건 버그가 아니라 의도**다. 다이아를 모르는 사이에
쓰지 않기 위한 것이므로 "매칭이 안 되니 임계값을 낮추자" 같은 수정을 하지 마라.

같은 이유로 장비 비교 팝업에서는 `장착`/`판매` 를 누르지 않는다. 빈 곳을 눌러 선택 없이 닫는다.

### 게임 화면 이미지를 저장소에 넣지 마라

`local/` 은 통째로 git 에서 제외된다. 템플릿·테스트 화면·진단 이미지는 전부 그 아래에 둔다.
저작물이기도 하고, 각자 자기 화면에서 뜨는 편이 정확하다.

새 템플릿이 필요하면 `template_specs` 에 **캡처 방법을 선언**해라. 캡처 마법사가 그걸 읽어
사용자를 안내한다. 선언하지 않으면 마법사 목록에 나타나지 않아 다른 사람은 쓸 수 없다.

이미지는 못 넣어도 **좌표는 넣을 수 있다**. `default_area` 에 실측 `NormRect` 를 적어 두면
마법사가 그 자리를 미리 잡은 채로 캡처 창을 연다. 사용자가 처음부터 드래그하지 않아도 된다.
`tips` 는 한 줄에 하나씩 뜨므로 문장을 이어 붙이지 말고 항목을 나눠 적어라.

목록은 소유자(모듈/퀘스트)의 `template_group` 으로 묶이고 `setup_order` 로 정렬된다.
`label` 은 묶음 안에서만 구별되면 되니 짧게 적어라 — 여러 퀘스트가 `퀘스트 창` 을 함께 쓴다.
묶음까지 붙인 이름이 필요하면 `spec.title` 을 써라.

### UI 와 실행 로직을 섞지 마라

`ccc/ui/` 는 tkinter 만 알고 판단을 하지 않는다. `ccc/app.py` 아래로는 tkinter 를 전혀 모른다.
판단이 필요하면 `AutomationApp` 에 메서드를 만들고 UI 는 그걸 부르기만 한다.

---

## 2. 실측값 — 왜 이 숫자인가

숫자를 만지기 전에 이 표를 보라. 대부분은 이미 여유가 좁은 값이다.

| 상수 | 위치 | 값 | 근거 |
| --- | --- | --- | --- |
| `RED_RATIO_THRESHOLD` | `quest/navigator.py` | 0.06 | 전투화면 0.0% / 팝업 84% — 여유가 아주 크다 |
| `MIN_RATIO` | `quest/panel.py` | 0.15 | 완료 금 86~95% / 진행중 회 16~92% |
| `MIN_MARGIN` | `quest/panel.py` | 1.5 | 우세한 쪽이 반대쪽의 1.5배는 되어야 확정 |
| `MIN_TEXT_RATIO` | `quest/panel.py` | 0.025 | 창 있음 0.035~0.080 / 없음 0.000~0.014. **빈 구간 한가운데** |
| `GREEN_RANGE` | `quest/panel.py` | S≥80, H 35~85 | 초록 창의 색 표본 85% 가 이 범위 (중앙 H 54 · S 130 · V 92) |
| `MIN_SCORE_MARGIN` | `quest/machine.py` | 0.08 | 쿠키↔펫 1등-2등 차이가 0.13~0.18 |
| `CLAIM_TEXT_CHANGE` | `quest/machine.py` | 0.05 | 같은 화면 0.0000 / 화면 전환 0.3061 |
| 글자 매칭 임계값 | `quest/definition.py` | 0.55 | 등록 0.675~0.989 / 미등록 0.362~0.370 (474px 폭) |
| 버튼 매칭 임계값 | 각 퀘스트 | 0.80~0.85 | 자기 화면 0.87~1.00 / 남의 화면 0.69~0.78 |

기본값은 **상수 한 곳에만** 적는다. `quest_runner` 가 `ctx.option` 의 폴백으로 숫자를 다시
쓰면 두 곳이 어긋나 상수를 고쳐도 안 먹는다 — `MAX_UNKNOWN_READS` 를 50 으로 바꿨는데
runner 가 20 을 넘겨 계속 20 으로 돌았다. 폴백에는 상수 이름을 써라.

### 캡처 비용이 모든 것을 지배한다

| 구간 | 시간 |
| --- | --- |
| **adb 캡처** | **579 ms** |
| **화면 캡처 (mss)** | **6.5 ms** |
| 인식 전체 (색 + X버튼 + 글자) | 0.3 ms |
| 템플릿 매칭 (범위 제한) | 6~9 ms |
| 템플릿 매칭 (전체 화면 탐색) | 166 ms |

인식은 다 합쳐도 10ms 안쪽이다. **성능을 고민한다면 캡처부터 보라.** 그리고 템플릿을 찾을 때는
반드시 `search` 범위를 줘라 — 전체 화면 탐색은 20배 이상 비싸다.

### 운영 해상도는 원본의 절반쯤이다

블루스택 창이 520x940 이면 1080x1920 의 48% 다. 화면 캡처를 켜는 순간 모든 인식이 그 크기로
돈다. 그래서 **새 템플릿은 반드시 50% 축소에서도 걸리는지 확인**해야 한다.

```bash
python3 tools/dev.py template test --names 새템플릿
```

한 줄짜리 짧은 글자는 50%에서 무너진다(실측 0.70). 이름 줄이 짧으면 아래 줄까지 함께 감싸
템플릿을 키워라. 테스트 픽스처가 절반 크기인 것도 이 때문이다 — 늘 축소 경로를 밟게 하려고.

---

## 3. 이 게임에서 배운 함정들

고치다가 같은 곳에 다시 빠지지 않도록 남긴다.

**퀘스트창은 반투명이다.** 뒤의 전투 배경이 비쳐서 스테이지마다 색이 달라진다. 주황색 사막
배경에서는 황금 패널과 색상이 겹친다. 그래서 고정 임계값 대신 **두 색군의 비율을 견줘 우세한
쪽**을 고른다.

**회색은 "채도가 낮다" 로 정의돼 있다.** 절전 모드처럼 채도만 낮은 화면도 퀘스트창으로 오인한다
(실측: 창이 없는데 회색 100%). 그래서 색을 보기 전에 **그 자리에 흰 글씨가 있는지** 먼저 본다.

**퀘스트창은 오른쪽 끝에 붙고 글자 길이에 따라 왼쪽으로 늘어난다.** 세로 위치도 1%쯤 흔들린다.
고정 사각형으로 자르지 말고 밴드 안에서 탐색해라.

**뒷부분이 같은 퀘스트 이름이 많다.** "쿠키 뽑기 10회 하기" 와 "펫 뽑기 10회 하기" 는 앞
한두 글자만 다르다. 임계값을 넘는 첫 후보를 채택하면 서로를 가로챈다(실측 오탐 0.82).
**전부 점수를 매겨 1등을 고르고 2등과 벌어졌는지 본다.**

**X 버튼이 없는 화면도 있다.** 레벨업 연출과 장비 비교 팝업은 "화면을 탭하세요" 로 넘긴다.
X 부재를 전투화면의 증거로 삼으면 영원히 대기한다. `빈 곳 탭 지점` 을 눌러 넘긴다.
반대로 **X 가 있는 팝업은 빈 곳 탭으로 안 닫힌다** — 재료 부족 안내가 그렇다. 빈 곳을
두드리기 전에 X 부터 확인해라(실측: 모르고 두드리다 68초를 버렸다).

**"확정 전" 과 "못 읽음" 은 다르다.** `StablePanelReader` 는 연속 2프레임이 같아야 확정하므로
첫 프레임은 언제나 확정 전이다. 이걸 판독 실패로 세고 화면을 건드리면(빈 곳 탭 → 판독기 초기화)
두 번째 프레임에 닿지 못해 **영영 확정되지 않는다.** 실측 로그에서 금 95% 가 20번 내리 찍히면서도
판정불가로 남았다. `PanelReading.settling` 으로 갈라 두었으니 없애지 마라.

**완료가 연달아 뜬다.** 보상을 받으면 다음 퀘스트가 또 완료일 수 있다. "황금이 아니게 되기" 만
기다리면 조건이 서지 않아 상한을 통째로 버린다(실측 4초). 색이 아니라 **글자가 바뀌었는지**를
봐라 — 퀘스트가 넘어가면 이름과 진행도가 같이 바뀐다.

**퀘스트창 영역을 좁게 잡으면 조용히 오인식한다.** 탐색 영역이 템플릿보다 작으면 매칭이
시도조차 되지 않고 0 점이 된다. 큰 템플릿만 빠지고 작은 것들끼리 1등을 뽑아 **엉뚱한 퀘스트로
확정된다**(실측: 장비 뽑기 화면에서 오븐 95px·상자 91px 가 빠지고 펫 37px 가 1등). 못 들어간
후보가 있으면 판별하지 않는다.

**게임이 스스로 절전 모드로 넘어간다.** 하단 네비게이션도 X 버튼도 없어져서 다른 판단이 전부
오염된다. `power_save` 모듈이 우선순위 5로 가장 먼저 처리한다.

**Vulkan 백엔드에서 게임이 몇 분마다 죽는다.** 크래시 스택이 `libvulkan_enc.so` 에서 났다.
OpenGL 로 바꾸면 안정적이다. 자동화 코드 문제로 오인하기 쉬우니 먼저 확인하라.

**글자 템플릿은 화면을 줄이지 말고 탐색 영역을 키워 맞춘다.** 얇은 획이 축소·이진화에서
뭉개진다. `vision/text.py` 의 `_match_scale` 이 그 처리를 한다(실측 0.70 → 0.82).

---

## 4. 무엇을 어디에 넣는가

| 넣을 것 | 위치 |
| --- | --- |
| 퀘스트 하나의 절차 | `ccc/quests/<이름>.py` — 파일 추가만으로 등록된다 |
| 여러 퀘스트가 공유하는 절차 | `ccc/quests/_<이름>.py` — 밑줄로 시작하면 등록되지 않는다 |
| 퀘스트와 무관한 상시 감시 | `ccc/modules/<이름>.py` |
| 게임 UI 요소의 위치 | `ccc/anchors.py` 의 `DEFAULTS` |
| 화면을 읽는 새 방법 | `ccc/vision/` |
| 상태 전이 규칙 | `ccc/quest/machine.py` |
| UI 와 실행을 잇는 것 | `ccc/app.py` |
| 반복 작업용 명령 | `tools/dev.py` 의 서브커맨드 |

---

## 5. 작업 흐름

### 새 퀘스트를 추가할 때

1. `goto-battle` 로 전투화면까지 간 뒤 그 퀘스트가 회색으로 뜬 상태를 만든다
2. `python3 tools/dev.py lines` 로 퀘스트창 글자 줄 좌표를 얻는다
   (첫 줄은 보통 "퀘스트 1319" 같은 번호다. 쓰지 마라)
3. `template save` 로 이름 템플릿을 뜬다. 숫자는 빼고, 짧으면 다음 줄까지 감싼다
4. `template test` 로 50% 점수를 확인한다
5. `identify` 로 다른 퀘스트와 0.08 이상 벌어지는지 확인한다
6. 실제로 절차를 손으로 밟으며 `shot` · `locate` 로 버튼 위치를 잡는다
7. `ccc/quests/` 에 파일을 만들고 `template_specs` 를 반드시 채운다
8. `tests/features/` 에 시나리오를 쓰고 픽스처를 `local/fixtures/frames/` 에 넣는다
9. `pytest` 와 `run --seconds 120` 으로 확인한다

### 버튼 좌표를 잡을 때

눈대중으로 재지 마라. 한 번 틀렸다.

```bash
python3 tools/dev.py locate orange --area 0.2 0.74 0.6 0.10 --save 이름
```

색 덩어리를 찾아 정규화 사각형을 알려 주고, `--save` 를 주면 바로 템플릿으로 저장한다.

### 인식이 이상할 때

```bash
python3 tools/dev.py diag                        # 지금 무엇으로 읽고 있는지
python3 tools/dev.py identify --from 저장된.png    # 퀘스트별 점수
python3 tools/dev.py shot --anchors              # 앵커가 제자리인지 눈으로
```

---

## 6. 테스트

BDD 시나리오는 한국어 Gherkin 이다. **사용자가 읽고 고칠 수 있는 명세**로 쓴다 — 퀘스트 절차를
이해한 대로 적어 두면 틀렸을 때 바로 드러난다.

- 공용 스텝은 `tests/conftest.py` 에 있다. 퀘스트별 테스트에는 **어디를 눌러야 하는지만** 둔다
- 가짜 ADB 만 쓰고 좌표 변환 · 템플릿 매칭 · 상태 전이는 실제 코드가 돈다
- 픽스처는 실제 캡처의 절반 크기다. 늘 축소 경로를 밟게 하려는 것이다
- 시간이 걸리는 규칙은 `FakeClock` 으로 시계를 돌려 확인한다 (1분 대기를 실제로 기다리지 않는다)
- `no_sleep` 픽스처는 `sleep` 과 `wait_until` 을 함께 갈아끼운다. 하나만 막으면 테스트가
  실제 시계로 상한을 다 태운다 (실측 3.8초 → 87초)

조사 때문에 스텝이 안 잡히는 일이 있다. "가" 와 "이" 처럼 갈리면 `parsers.re` 로 둘 다 받아라.

---

## 7. 셸 명령을 쓸 때

파이썬 코드를 힙독(`python3 - <<'PY'`)으로 실행하지 마라. 본문의 `|` 와 `;` 가 셸 메타문자로
읽혀 권한 검사기가 매번 낯선 복합 명령으로 본다(실측: Bash 호출의 45%가 이 경우였다).

반복할 일이면 `tools/dev.py` 에 서브커맨드로 넣어라. 단순 명령 하나가 되어 권한 규칙
`Bash(python3 tools/dev.py *)` 한 줄로 덮인다.

---

## 8. 지금 상태

- 퀘스트 4개: 오븐 장비 뽑기 · 가방 상자 사용 · 쿠키 뽑기 · 펫 뽑기
- 모듈 4개: 절전 모드 해제 · 게임 유지 · 팝업 닫기(예시) · 퀘스트 자동화
- 템플릿 11개 (`local/templates/`, git 제외) + 번들 조각 2개 (`ccc/assets/`)
- BDD 시나리오 36개
- 가이드 영상 2개 (`docs/`, Git LFS)
- 라이브 검증: 30분 연속 무중단. 보상 수령 · 네 퀘스트 수행 · 절전 모드 해제 ·
  Auto 대기 후 복귀 · 상자 재선택 · 미등록 퀘스트 라운드 모두 확인

### 알려진 한계

- 판별이 완벽하지 않다. 등록되지 않은 퀘스트가 기존 것과 이름이 비슷하면 오판할 수 있고,
  이건 템플릿으로도 OCR 로도 못 막았다(둘 다 3번 중 1번 오판). 실행 단계 안전장치가 방어선이다
- `bag_treasure_box` 는 특정 계정의 아이템이다. 그 상자가 없으면 그 퀘스트는 못 한다
- 앵커 기본값은 1080x1920 세로 화면 기준이다. 다른 비율이면 `영역 보정` 이 필요하다
- **블루스택 창이 작으면 모든 글자 템플릿이 아슬아슬해진다.** 474~506px 폭(원본의 44~47%)에서
  이름 한 줄은 13px 남짓이라, 잘 맞는 템플릿도 0.68 대에 머문다. 임계값 0.55 는 그 상황을
  버티는 값이다. 창을 키우고 템플릿을 다시 뜨면 0.9 대로 올라간다
- `oven_grow` 는 그 화면 스크린샷이 없어 위치를 못 쟀다. 전체 탐색이라 한 번에 166ms 든다.
  레벨업 확인창을 만나면 `local/oven_level_up/` 에 화면 세 장이 쌓이니 그때 재서 좁혀라
- 뽑기 결과창 닫기에 매번 3~4초가 든다(`닫기 1/6 → 3/6`). 막힘은 아니고 속도 문제다
